[phases.setup]
nixPkgs = ["...", "python313"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[start]
cmd = "python -m playwright install chromium && python app.py"
